# Design previews are regenerated by tools/preview.py from the theme + wordmark.
# The wordmark PNGs themselves ARE tracked: the theme needs them at boot, and
# the phone has no Python or fonts to render them at install time.
taos/preview.gif
taos/preview.png
